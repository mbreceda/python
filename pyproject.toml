[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.3.3",
    "lxml>=5.1.1",
    "rich>=15.0.0",
]

[tool.black]
target-version = ["py312"]
